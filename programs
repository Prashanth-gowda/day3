Write a program to get two integers from the user and print the sum of the two integers?

#include <stdio.h>

int input()
{
    int a;
    printf("enter the integer:");
    scanf("%d",&a);
    
    return a;
}

int compute(int c,int d)
{
    int sum;
    sum = c + d;
    return sum;
}

int output(int s)
{
   printf("%d \n ",s);
}


int main()
{
    int a,b,c,d,res;
    c = input();
    d = input();
    res = compute(c,d);
    output(res);
    
}
